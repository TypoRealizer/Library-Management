import React from "react";

export default function App(){
  return (
    <div style={{padding:20}}>
      <h2>Library Management — Frontend Skeleton</h2>
      <p>If you see this page, the frontend container is running.</p>
      <p>When backend is ready, it will call <code>/health</code>.</p>
    </div>
  );
}
